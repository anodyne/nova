* changed the registration code to point to a new sub-domain on the AP site
* updated the readme
* updated the install and update files for the 1.2.5 release
* fixed bugs where deactivated users retained their account flags (sysadmin, game master, etc) and reactivated users didn't have a reasonable access role
* updated the readme
* updated the gitignore file
* attempting to nail down the illegal operand fatal error that keeps sneaking up because of corrupt session data
* updating the various version and config files for a new release
* fixed bug where old specification items wouldn't accept the values of new fields because there wasn't a field in the data table to start with